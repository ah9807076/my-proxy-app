const express = require('express'); const axios = require('axios'); const cors = require('cors'); const app = express(); const port = process.env.PORT || 3000; app.use(cors()); app.get('/fetch', async (req, res) => { try { const response = await axios.get('https://eg.waseet.net/ar/post/blwmar-sydy-bd-alrhmn-blumar-10738241'); res.send(response.data); } catch (error) { res.status(500).send('Error fetching the URL'); } }); app.listen(port, () => { console.log(); });
